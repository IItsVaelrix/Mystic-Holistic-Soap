# Optional overrides for the Saponification Bench server.
# The app runs fully offline with no external services or API keys.

# Network binding. Defaults to localhost — the server has no authentication,
# so only change HOST if you understand the exposure.
# HOST="127.0.0.1"
# PORT="3000"
